

// on ecoute le bouton 'se connecter'
const loginSubmit = document.getElementById("loginSubmit")
loginSubmit.addEventListener('click', () => {

    // on recuperer les données du formulaire
    const inputEmail = document.getElementById("email")
    const inputPassword = document.getElementById("pass")
})

// verifier que les champs ne sont pas vides
// verifier format du mail et du mot de passe (avec regExp)
// Envoyer requete poste avec les données
// si reponse du serveur = 200, on connecte l'utilise
    // on enregistre le token dans "sessionStorage"
    // On retourne vers la page d'accueil
// sinon, on met message non valide



